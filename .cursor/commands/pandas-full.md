# Full pandas contribution flow

Follow `pandas-contribution-copilot` (`.cursor/skills/pandas-contribution-copilot/SKILL.md`).

Run the full workflow in order: start → inspect → plan → implement (if requested) → verify → ship (auto after verify succeeds).

Assume the user has already chosen an issue. Do not select issues automatically.

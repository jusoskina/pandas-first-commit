# Plan pandas contribution

Follow `pandas-plan-contribution` (`.cursor/skills/pandas-plan-contribution/SKILL.md`).

Classify risk (stage 3), then create a test-first plan (stage 4). Output both the **Risk classification** and **Test-first plan** templates. Apply change-type rules from `pandas-contribution-core`.
